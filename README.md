to install the `add_tooll` MCP server, run the following command.
"add_tool": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/itumasankar/mcpserverdeployment.git",
        "mcp-server"
      ]
    }